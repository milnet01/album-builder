# G-20260921-1e43 — review-doc (draft v2) on Spec 20

**Subject:** `docs/specs/20-mpris-tray-integration.md` (43,823 bytes)
**Kind:** spec · **Arm:** draft · **Date:** 2026-09-21
**Trigger:** commit `60e7b82` widened the MPRIS `SupportedMimeTypes` list and the
extension list. Only Spec 01 was gated in that change, so this hunk was ungated.
Named line: the `SupportedMimeTypes` list, which gained three entries.

## Deviations from the skill as written

- **No `Genre:` line.** Step 1 says stop and ask. No document in this project has
  one. Declared `spec` and proceeded; recorded here rather than silently inferred.
- **Checker ran degraded**, and said so: no `.claude/workflow.json`, so
  required-section and ignore-path checks were skipped; no `Genre:` line, so the
  gate, ids, lineref, path, sections, symbol and version checks did not run.
- **Briefs carried no read budget.** The § The lane budget section was added to the
  skill mid-run. This run measures the version without it.
- **Diff pass dispatched four lanes, not six.** `invent` and `mechanism` were
  dropped: the hunks invent no name other code binds to, and mandate nothing
  runnable. Recorded as a deviation, not a silent saving.

## Pass 1 — full

Lanes: `false`, `conflict`, `uncited`, `invent`, `untestable`, `mechanism`.

| Lane | Tokens | Tool uses | Raised |
|---|---|---|---|
| `invent` | 90,688 | 2 | 0 |
| `false` | 129,608 | 32 | 0 |
| `uncited` | 103,444 | 30 | 2 |
| `conflict` | 116,669 | 5 | 0 |
| `untestable` | 111,160 | 9 | 4 |
| `mechanism` | 162,374 | 30 | 0 |
| **total** | **713,943** | | **6** |

**Mechanism: ran.** Live session bus present. The lane ran the suite, then set
`AB_INTEGRATION_DBUS=1` and ran it again (24/24, 0 skipped), then probed the
registered object with `busctl` as an external client — including the `GetAll`
read that historically SIGABRT'd. Process survived; wire types `x`, `o`, `as`
confirmed. `SetPosition` race-guard and range-guard, `Seek` offset math, `Volume`
write-forwarding and the six-action tray menu all matched under live execution.

**Could not reach:** full-app `MainWindow` construction/teardown against a running
instance; hardware media-key routing and the Plasma applet's own rendering.

**Fixed (5):**
1. `xesam:artist` mapping never named the `Track` schema source. An implementer
   must choose `artist` vs `album_artist`; the latter falls back to the former when
   TPE2 is absent, so a wrong choice looks right except on compilations. Named
   `domain/track.py`, owned by Spec 01, and added `01` to `Depends on:`.
2. TC-20-03's wire-type half is discharged only by a test nothing enables.
3. TC-20-05 carries the same gap.
4. TC-20-09 asserted bus detection via a test that assigns `available = False`
   itself before asserting.
5. TC-20-06's "mute independence" observable was a code comment.

**Dismissed (1):** `PlaybackController`'s file path unnamed. Verified true; the
class name is unique and Spec 15 is already in `Depends on:`, so nothing is built
wrong. Out of scope.

**Author corrections, caught before the diff pass, not lane findings:** the TC-20-03
fix first mandated that the gate set the env var — inventing CI policy the finding
did not support — and restated a disclosure already present at line 516.

## Pass 2 — diff-scoped

| Lane | Tokens | Tool uses | Raised |
|---|---|---|---|
| `false` | 80,494 | 21 | 0 |
| `conflict` | 76,343 | 9 | 0 |
| `uncited` | 80,871 | 18 | 0 |
| `untestable` | 103,136 | 13 | 2 |
| **total** | **340,844** | | **2** |

Both findings landed on text pass 1 had written, which is the pattern this
project's own loop logs predicted.

**Fixed (2):**
6. TC-20-09's new instruction named what to assert but no route to the state.
   `MprisService.__init__` takes no bus parameter. The lane called it unreachable
   and said it could not run code to confirm. Running it settled it:
   `QDBusConnection.sessionBus` is patchable at class level on this build, so the
   gap is a missing seam, not impossibility. The seam is now named.
7. TC-20-03's replacement was disclosure, not requirement. Labelled as such, so it
   is not mistaken for coverage. Whether the gate should enable the variable is
   left open — that decision is the project's.

## Cost

Agents: 10. Agent tokens: **1,054,787**.

Parent session over the run: 124 assistant turns.

| Component | Tokens |
|---|---|
| input | 248 |
| cache creation | 286,908 |
| cache read | 29,405,914 |
| output | 140,167 |
| raw sum | 29,833,237 |
| new tokens only | 427,323 |
| billed-equivalent (reads weighted 0.1) | 3,367,914 |

The row in `run-costs.tsv` holds the raw sum, which is 98.6% cache reads and
therefore tracks turn count times context size rather than review cost. Mean
context re-read per turn: 237,144. This is a defect in the column's definition,
not in the run.

## Outcome

Seven findings fixed, one dismissed. No third pass; the skill ends after the
diff-scoped pass. The two pass-2 fixes are themselves unreviewed, which is that
design's accepted endpoint.

## Three lane-cost figures that disagree

The `agent-cost` hook fired unprompted and wrote `docs/reviews/agent-costs.jsonl`.
Its figures disagree with the task-notification `subagent_tokens` by up to 46x.

| Source | Total across 10 agents |
|---|---|
| task notification `subagent_tokens` | 1,054,787 |
| hook, new tokens (input + cache creation + output) | 2,550,020 |
| hook, raw total (including cache reads) | 27,136,500 |

A 26x spread between the outer two. Any rule that says "record what the lane cost"
must name which of these it means.

Two defects in the hook's output as it stands:

- One agent (the `conflict` lane) has two entries. Summing the file double-counts it.
- All eleven entries carry no usable run id — seven have no `run` key, four have
  `"run": null` — so lane cost cannot be joined to the run that caused it.
